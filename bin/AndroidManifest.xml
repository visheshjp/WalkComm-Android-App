<?xml version="1.0" encoding="utf-8"?>
<manifest xmlns:android="http://schemas.android.com/apk/res/android"
    package="com.example.walkcomm"
    android:versionCode="1"
    android:versionName="1.0" >

    <uses-sdk android:minSdkVersion="8" />
    <uses-permission android:name="android.permission.WRITE_EXTERNAL_STORAGE"/>
    <uses-permission android:name="android.permission.RECORD_AUDIO" />

    <application
        android:icon="@drawable/logo"
        android:label="@string/app_name" >
        <activity
            android:label="@string/app_name"
            android:name="com.example.walkcomm.DashboardActivity" >
            <intent-filter >
                <action android:name="android.intent.action.MAIN" />

                <category android:name="android.intent.category.LAUNCHER" />
            </intent-filter>
        </activity>
        
        <!--  Login Activity -->
        <activity
            android:label="Welcome To WalkComm" 
            android:name="com.example.walkcomm.LoginActivity"></activity>

        <!--  Register Activity -->
        <activity
            android:label="Register New Account" 
            android:name="com.example.walkcomm.RegisterActivity"></activity>
        
        <!--  Recording Activity -->
        <activity
            android:label="Record Messages" 
            android:name="com.example.walkcomm.RecordingActivity"></activity>
        
        <!--  Message Activity -->
        <activity
            android:label="View Messages" 
            android:name="com.example.walkcomm.MessageActivity"></activity>
        
    </application>
    
    <!-- Allow to connect with internet -->
	<uses-permission android:name="android.permission.INTERNET" />

</manifest>